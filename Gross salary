#include <stdio.h>

int main ()

{
    int gs,bs,ta,da;

    printf("Enter basic salary:");
    scanf("%d", &bs);

    da=(10*bs)/100;
    ta=(12*bs)/100;

    gs=bs+ta+da;

    printf("Gross salary: %d", gs);

    return 0;
}
