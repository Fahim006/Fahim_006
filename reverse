#include <stdio.h>

int main ()

{
    int num, r, reverse = 0;


    printf ("Enter your number\n");
    scanf ("%d", &num);

    while (num){
        r = num%10;
        reverse=reverse*10+r;

        num = num/10;
    }
    printf ("reversed of number is %d\n", reverse);

    return 0;
}
