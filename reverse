#include <stdio.h>

int main ()

{
    int num, r, revense = 0;


    printf ("Enter your number\n");
    scanf ("%d", &num);

    while (num){
        r = num%10;
        revense=revense*10+r;

        num = num/10;
    }
    printf ("revensed of number is %d\n", revense);

    return 0;
}
